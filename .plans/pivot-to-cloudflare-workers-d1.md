# Plan: Pivot Nusend to Cloudflare Workers + D1

## Implementation Progress

Tracker for the implementation run started 2026-07-06.

- [x] Phase 1: Repo flattening and minimal Cloudflare scaffold
- [x] Phase 2: Fresh D1 schema (`migrations/0001_init.sql`)
- [x] Phase 3: Workers testing foundation (vitest-pool-workers, ported pure tests)
- [x] Phase 4: API-key auth (middleware, create-api-key script, tests)
- [x] Phase 5: Hono Worker app (app assembly, /health, public stubs, /api boundary)
- [x] Phase 6: D1 queue + Dispatcher DO (jobs port, runner, processors, watchdog, tests)
- [x] Phase 7: Mailings migration (create path, marketing guard, expand_mailing, GET :id, tests)
- [x] Phase 8: Documentation rewrite (PROJECT.md, README.md)
- [x] Final validation (`pnpm install`, `pnpm format`, `pnpm check`, `pnpm test`, wrangler dev smoke)
- [x] Final independent review (Pi session, 2 rounds; all findings fixed or rejected with documented reasons)

### Log

- 2026-07-06: Read full plan and current codebase. Launched docs research for current wrangler/vitest-pool-workers versions and APIs. Review CLI: `pi` 0.80.3 available (preferred external reviewer since runtime is Claude).
- 2026-07-06: Research finding: `@cloudflare/vitest-pool-workers` 0.18 (Vitest 4) replaced `defineWorkersConfig`/`SELF` with a `cloudflareTest()` Vite plugin; `env`/`exports` now come from `cloudflare:workers`. Implemented against the current API.
- 2026-07-06: Implemented all 8 phases sequentially (single tightly-coupled package; no parallel implementation subagents — documented decision). Key implementation choices beyond the plan text:
  - Send-job ids are deterministic (`send_delivery:<deliveryId>`) and created via `INSERT OR IGNORE ... SELECT FROM deliveries`, so retried expansion chunks cannot duplicate jobs even though delivery UUIDs are random (the unique `(mailing_id, email)` index keeps the first delivery row, and the job id derives from the surviving row).
  - Next expansion cursor jobs also use deterministic ids (`expand_mailing:<mailingId>:<lastContactId>`), preventing duplicate expansion chains on retry.
  - `claimJobs` requires an explicit kind list and claims nothing when it is empty (registry-driven safety).
  - `drainOnce` from the old runner was dropped: the Dispatcher processes bounded slices per alarm and re-arms instead.
  - `jobs.priority`: transactional send jobs get 10, campaign/expansion work 0.
  - `createMailing` implements the list path (mailing + `expand_mailing` job, 202) at the function level; the route-level marketing guard blocks it for API callers until unsubscribe exists.
  - Node script `scripts/create-api-key.ts` runs via `node` (Node 26 native TS); `@types/node` added for scripts/config only (not in tsconfig `types`).
  - pnpm 11 uses `pnpm-workspace.yaml` as its config file, so the file remains (build allowances), even though the workspace is flattened.
  - `.env.example` deleted (no runtime env vars in v1); `worker-configuration.d.ts` generated by `wrangler types` and gitignored; `typecheck` runs `wrangler types` first.
  - Tests truncate all tables in `beforeEach` because vitest-pool-workers isolates storage per test file, not per test.
- 2026-07-06: Validation: `pnpm check` green (format, lint with pre-existing-style `no-await-in-loop` warnings only, typecheck, 55 tests in Workers pool). `wrangler d1 migrations apply --local` OK. `wrangler dev` smoke: /health 200 public, /api 401 without key, 501 public stubs, key script + d1 insert + authenticated POST /api/mailings 201 + GET :id counts all verified.
- 2026-07-06: Reviews consolidated: all phases landed in one continuous pass, so a single comprehensive Pi review session covers the full diff instead of per-phase reviews (documented deviation from the per-step cadence).
- 2026-07-06: Pi review round 1 (session 9714425e-10f9-42f3-965f-ff97e55a3923) findings and outcomes:
  - FIXED (must-fix): list expansion was scheduled at `scheduledAt`; now the initial `expand_mailing` job runs immediately (audience snapshot at creation), while send jobs keep `run_at = scheduled_at`. Explicit priority 0 for expansion jobs.
  - FIXED (must-fix): `POST /api/mailings` awaited `dispatcher.poke()` after the D1 commit, so a transient DO RPC failure would 500 a committed create (retry → duplicate mailing). Poke now runs detached via `executionCtx.waitUntil` with logged errors; cron watchdog is the recovery path.
  - FIXED: Bearer scheme parsing now case-insensitive (RFC 9110); tests cover Bearer/bearer/BEARER.
  - FIXED: `create-api-key.ts` shell-escapes the printed wrangler command.
  - FIXED (test gaps): added suppression scope-matrix + chunked-lookup tests, 100-recipient cap creation test (chunked inserts) + 101-rejection, 95-member expansion chunk test (splits delivery inserts and send-job IN chunks), and an assertion that mailings stay `scheduled` after expansion completes.
  - REJECTED with reason: no poke-failure injection test (vitest pool cannot cleanly inject a failing DO stub; response is provably independent of poke via waitUntil). `.plans/*.html` kept (repo convention tracks plan HTML exports). Direct-path poke kept but detached (matches plan flow; needed once the sender registers).
  - Validation after fixes: `pnpm check` green, 59 tests passing.
- 2026-07-06: Cleanup pass (/simplify, 4 parallel review agents: reuse/simplification/efficiency/altitude). Applied: key script now imports `hashApiKey`/`apiKeyPrefix` from `src/auth/api-keys.ts` (was a byte-identical reimplementation); shared `placeholders()` + `maxInLookupValues` in `lib/d1-batch.ts` replace three hand-rolled IN-chunk idioms; shared `errorResponse` in `lib/errors.ts` replaces six inline error envelopes; new `buildAudienceStatements` in `pipeline.ts` dedups the identical suppress→deliveries→send-jobs sequence in create-mailing and expand-mailing; `GET /:id` read model extracted to `mailings/get-mailing.ts` (queries run in parallel); watchdog pokes unconditionally (poke no-ops when idle) removing a duplicate MIN(run_at) query and the now-dead `hasQueuedJobsByKinds`; contact+suppression lookups parallelized in the direct create path; redundant `send_attempts_delivery_id_idx` dropped from the migration; dispatcher passes kinds into `ensureAlarm` instead of rebuilding the registry; `JobProcessor` type defined once; priority policy consolidated in `pipeline.ts` (`expandJobPriority`); internal-only exports unexported (`jobKinds`, `normalizeEmail`, `maxExplicitRecipients`, `sendJobPriority`, d1-batch internals); `.gitignore` rewritten for the Workers stack (`.dev.vars` instead of `.env`, Bun/SQLite-era patterns removed). Skipped with reasons: moving the marketing guard into `createMailing` (plan explicitly wants the API-boundary guard while keeping the domain pipeline testable pre-unsubscribe); deleting `.oxfmtrc.json` (oxfmt errors without a config file — agent finding was wrong); shared job-columns constant (INSERT...SELECT vs VALUES differ enough); batching test seed loops (test-only, negligible). Validation: `pnpm check` green, 60 tests, key script re-smoke-tested incl. quote-escaping.
- 2026-07-06: Pi review round 2 confirmed all round-1 must-fix/should-fix items resolved (reviewer independently re-ran the tests). One remaining caveat: expansion was a live cursor, not a create-time audience snapshot. FIXED: `readMemberChunk` now filters `list_memberships.subscribed_at <= mailings.created_at` (members added after creation are excluded; members who unsubscribe before their chunk are deliberately dropped), with a dedicated test and PROJECT.md documentation. `pnpm check` green, 60 tests.

## Summary

Aggressively migrate Nusend from the current Bun/VPS/local-SQLite/Better-Auth direction to a clean Cloudflare-first foundation, as if Cloudflare Workers and D1 were the target from day one.

This migration is **not** the full product implementation. It should establish the new architecture and migrate the existing working foundation:

- API-only Cloudflare Worker with Hono.
- D1 as durable source of truth.
- API-key-only authentication.
- Durable Object dispatcher scaffolding for background work.
- D1 job queue primitives ported to async D1.
- Existing direct-recipient transactional `POST /api/mailings` migrated to D1.
- List/campaign expansion architecture prepared with `expand_mailing`, while marketing creation remains blocked until unsubscribe support exists.
- Public route contracts for unsubscribe and SES/SNS webhook.
- `PROJECT.md` rewritten to the new Cloudflare-first vision.

No SES sending, real unsubscribe token handling, SNS signature verification, template API, placeholder rendering, R2 assets, dashboard, or CLI should be completed in this migration.

## Confirmed Decisions

- Cloudflare-first, not VPS/Bun.
- No Bun runtime in v1.
- API-only v1; no admin web UI.
- CLI is future work and should be a thin HTTP wrapper only.
- No orgs, workspaces, teams, sessions, Google OAuth, or Better Auth.
- Single deployment = single tenant.
- Multiple named API keys may exist, but no scopes/roles for v1.
- API keys should use `Authorization: Bearer nusend_...` as the primary header.
- Public unauthenticated route families are allowed for:
  - unsubscribe
  - SES/SNS webhook
  - health checks
- Durable Objects are acceptable; Workers Paid plan is acceptable.
- Cron cadence target: every 5 minutes (`*/5 * * * *`).
- Cron is watchdog/janitor, not the primary sender loop.
- Use one downstream sending pipeline based on `deliveries` + `send_delivery` jobs.
- Direct transactional/small-recipient sends materialize deliveries immediately.
- List/campaign sends use background `expand_mailing` to snapshot list recipients into deliveries and `send_delivery` jobs.
- No Cloudflare Queues for v1. D1 jobs remain the source of truth; one Dispatcher Durable Object will eventually enforce global SES rate.
- Drop the current unused `templates` table from the fresh schema. Document future templates as copy-on-create convenience over immutable mailings.
- Delete the old untracked VPS/Bun SES worker plan files.

## Research and Architecture Findings

### Cloudflare Workers and D1

- Workers use module exports such as `export default { fetch, scheduled }`.
- D1 is accessed through environment bindings such as `env.DB`.
- D1 statements are async: `prepare(...).bind(...).run()/first()/all()/raw()`.
- D1 supports anonymous and ordered positional params (`?`, `?NNN`), not the current named params (`$id`, `$now`).
- `env.DB.batch([...])` executes statements sequentially as a transaction and rolls back on failure.
- D1 otherwise operates in auto-commit mode; no interactive transaction callback like Bun SQLite.
- D1 limits that shape the design:
  - 100 bound params per query.
  - 100 KB SQL statement length.
  - 2 MB max string/BLOB/row size.
  - 30s max SQL query duration.
  - 1,000 D1 queries per Worker invocation on paid / 50 on free.
- D1 is single-threaded per database and queues concurrent work; too much concurrent work can return overload errors.
- D1 migrations are Wrangler-managed forward SQL files in `migrations/`, tracked by `d1_migrations`.

### Durable Objects and alarms

- Durable Object alarms are official Cloudflare functionality.
- A Durable Object can call `ctx.storage.setAlarm(timestamp)` to have Cloudflare wake it later via `alarm()`.
- Each Durable Object can have one alarm scheduled at a time.
- Alarm execution is at least once and automatically retried if `alarm()` throws.
- Only one `alarm()` invocation runs at a time per Durable Object instance.
- A Durable Object is not a permanent process; it should process bounded slices, persist durable state in D1, then self-schedule another alarm if work remains.

### SES sending rate implications

- SES send rate is account/region-global, e.g. around 14/sec for some accounts but lower in sandbox.
- The hard problem is global SES rate limiting, not just queueing.
- Cloudflare Queues can deliver high throughput but do not provide the single global serialization point needed for SES rate limiting.
- A single global Dispatcher Durable Object is the right primitive for the eventual SES token bucket / rate limiter.
- Cron every 5 minutes should be a watchdog that restarts/pokes the dispatcher if needed, not the core send loop.

### Templates/content model

- `mailings.subject/html/text` plus `deliveries.vars_json` already represent a template instantiation.
- Stored named templates are future convenience for reuse, not needed for the foundation.
- Mailings must remain immutable snapshots; if templates are added later, template content is copied into `mailings` at creation time.
- Future placeholder contract to document now:
  - syntax: `{{ path.to.value }}`
  - values resolved from `deliveries.vars_json`
  - HTML escaped by default
  - no loops, conditionals, or expressions in v1
  - missing variable fails the delivery terminally
  - Markdown source is never stored in Nusend; `mdtoemail` remains external

## Current-State Findings

Current project is a pnpm workspace with `apps/service` and empty `apps/cli`.

### Valuable code/concepts to preserve or port

- Hono app approach.
- `mailings` + `deliveries` core model.
- Contacts/lists/suppressions model.
- `send_attempts` and `ses_events` concepts.
- Queue state concepts: claim, lease, retry, dead-letter, expired lease release.
- Pure helpers:
  - `apps/service/src/mailings/validation.ts`
  - `apps/service/src/queue/backoff.ts`
  - `apps/service/src/queue/time.ts`
- Suppression policy logic from `apps/service/src/mailings/create-mailing.ts`.

### Code/files to delete or replace

- Bun server entrypoint: `apps/service/src/main.ts`.
- Bun SQLite DB layer: `apps/service/src/db/index.ts`.
- Custom migration system:
  - `apps/service/src/db/migrate.ts`
  - `apps/service/src/db/migration-files.ts`
  - related tests
- Config parser designed for Bun/VPS/Better Auth:
  - `apps/service/src/config.ts`
  - `apps/service/src/config.test.ts`
- Better Auth implementation:
  - `apps/service/src/auth/*`
  - `apps/service/src/db/migrations/sql/0002_auth.sql`
  - Better Auth dependencies
- Bun subprocess test harness:
  - `apps/service/src/testing/bun-scenario.ts`
- Existing app files should be replaced, not blindly moved:
  - `apps/service/src/app.ts`
  - `apps/service/src/app.test.ts`
- Empty CLI app:
  - `apps/cli/`
- Workspace shell if flattening:
  - `apps/service/package.json`
  - `apps/service/tsconfig.json`
  - `pnpm-workspace.yaml`
  - `tsconfig.base.json`
- Old untracked VPS/Bun SES plan files:
  - `.plans/add-ses-sender-runtime-worker.md`
  - `.plans/add-ses-sender-runtime-worker.html`
- Root `vitest.config.ts` should be replaced with a Workers-pool config.
- `pnpm-lock.yaml` should be regenerated after dependencies change.

## Chosen Target Structure

Flatten to a single Cloudflare Worker package:

```txt
nusend/
  wrangler.jsonc
  package.json
  tsconfig.json
  vitest.config.ts
  migrations/
    0001_init.sql
  scripts/
    create-api-key.ts
  src/
    index.ts
    app.ts
    bindings.ts
    auth/
      api-keys.ts
    mailings/
      validation.ts
      routes.ts
      create-mailing.ts
      expand-mailing.ts
    queue/
      jobs.ts
      runner.ts
      backoff.ts
      time.ts
    dispatch/
      dispatcher.ts
      processors.ts
    public-routes/
      unsubscribe.ts
      ses-webhook.ts
    lib/
      d1-batch.ts
  PROJECT.md
  README.md
  .plans/
```

Rationale:

- A greenfield Cloudflare Worker project would not use a monorepo with an empty CLI package.
- Wrangler, D1 migrations, and Workers testing are simplest at project root.
- Reintroducing a workspace later is easy when a real CLI exists.

## Target Infrastructure

### Cloudflare bindings

`wrangler.jsonc` should define:

- Worker main: `src/index.ts`
- current `compatibility_date`
- D1 binding: `DB`
- Durable Object binding: `DISPATCHER`
- Durable Object class migration for `Dispatcher`
- Cron trigger: `*/5 * * * *`
- observability enabled if desired

Example shape:

```jsonc
{
  "name": "nusend",
  "main": "src/index.ts",
  "compatibility_date": "2026-07-06",
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "nusend",
      "database_id": "replace-with-d1-database-id",
      "migrations_dir": "migrations"
    }
  ],
  "durable_objects": {
    "bindings": [
      { "name": "DISPATCHER", "class_name": "Dispatcher" }
    ]
  },
  "migrations": [
    { "tag": "v1", "new_sqlite_classes": ["Dispatcher"] }
  ],
  "triggers": {
    "crons": ["*/5 * * * *"]
  },
  "observability": { "enabled": true }
}
```

Notes:

- Phase 1 must add a minimal compilable `Dispatcher` class and Worker export immediately so `wrangler dev` and Workers tests can load the config from the start.
- No `nodejs_compat` should be needed for this migration.
- SES signing via `aws4fetch` can be added later without AWS SDK v3 bundle overhead.
- Exact D1 database ID may require a `wrangler d1 create nusend` setup step.

## Fresh D1 Schema

Create `migrations/0001_init.sql` as a fresh Wrangler migration. Do not preserve `-- migrate:up` / `-- migrate:down` markers.

### Include tables

- `api_keys`
- `lists`
- `contacts`
- `list_memberships`
- `mailings`
- `deliveries`
- `suppressions`
- `jobs`
- `send_attempts`
- `ses_events`

### Drop/defer tables

- Drop current `templates` table for now.
- Drop all Better Auth tables.

### API keys

```sql
CREATE TABLE api_keys (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  key_prefix TEXT NOT NULL,
  key_hash TEXT NOT NULL,
  created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ', 'now')),
  revoked_at TEXT
);

CREATE UNIQUE INDEX api_keys_key_hash_idx ON api_keys (key_hash);
CREATE INDEX api_keys_revoked_at_idx ON api_keys (revoked_at);
```

No `last_used_at` in v1 to avoid a D1 write on every authenticated request.

### Mailings

Keep `mailings` as immutable content snapshot:

- `id`
- `purpose`
- `state`
- `name`
- `subject`
- `html`
- `text`
- `list_id`
- `scheduled_at`
- timestamps

Do not add `template_id` yet.

### Deliveries

Add a domain invariant preventing duplicate recipients for the same mailing:

```sql
CREATE UNIQUE INDEX deliveries_mailing_email_idx ON deliveries (mailing_id, email);
```

Rationale:

- Expansion alarms are at least once.
- Worker/DO crashes or retries must not create duplicate deliveries/send jobs for the same mailing+recipient.
- Emails are normalized to lowercase and the column should remain `COLLATE NOCASE`.

### Jobs

Add flexible job support:

```sql
CREATE TABLE jobs (
  id TEXT PRIMARY KEY,
  kind TEXT NOT NULL,
  state TEXT NOT NULL,
  priority INTEGER NOT NULL DEFAULT 0,
  payload_json TEXT,
  run_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ', 'now')),
  attempts INTEGER NOT NULL DEFAULT 0 CHECK (attempts >= 0),
  max_attempts INTEGER NOT NULL DEFAULT 10 CHECK (max_attempts > 0),
  locked_by TEXT,
  locked_until TEXT,
  ref_id TEXT NOT NULL,
  last_error TEXT,
  created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ', 'now')),
  updated_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ', 'now'))
);

CREATE INDEX jobs_state_priority_run_at_idx ON jobs (state, priority DESC, run_at);
CREATE INDEX jobs_locked_until_idx ON jobs (locked_until);
CREATE INDEX jobs_kind_ref_id_idx ON jobs (kind, ref_id);
```

Rationale:

- `payload_json` supports expansion cursors and future job parameters.
- `priority` prevents large campaigns from blocking transactional send jobs later.
- Do not use a `CHECK` on `jobs.kind`; future job kinds such as `expand_mailing`, `send_delivery`, `process_ses_event`, `finalize_mailing` should not require table rebuilds.
- Validate job kinds/states in TypeScript.

### Enum CHECK constraints

Avoid SQL `CHECK` constraints for evolvable workflow states/kinds:

- `jobs.kind`
- `jobs.state`
- `mailings.state`
- `deliveries.status`

Keep stable structural checks where useful:

- `mailings.purpose IN ('transactional', 'marketing')`
- suppressions scope/list consistency
- numeric attempts checks

Rationale: SQLite/D1 cannot easily alter existing CHECK constraints without table rebuilds.

## Auth Design

Implement simple API-key auth in `src/auth/api-keys.ts`.

Behavior:

- API callers send `Authorization: Bearer nusend_...`.
- Middleware hashes the provided key with SHA-256.
- Lookup `api_keys.key_hash`.
- Reject missing, invalid, or revoked keys with `401`.
- Set a minimal principal such as `{ kind: 'api_key', apiKeyId }` if needed.

Use SHA-256, not bcrypt:

- Keys are high-entropy server-generated random secrets.
- Slow password hashing protects low-entropy human passwords, not 256-bit random tokens.
- SHA-256 avoids unnecessary Worker CPU cost.

Add `scripts/create-api-key.ts`:

- generates a high-entropy key with prefix `nusend_`
- computes hash
- prints plaintext key once
- prints a `wrangler d1 execute` command or SQL insert for local/remote use
- does not add an admin endpoint

## Mailings and Pipeline Design

### Direct-recipient path

For transactional/small direct sends:

```txt
POST /api/mailings { recipients: [...] }
  -> validate max recipients
  -> create mailing
  -> resolve optional contact ids
  -> apply suppression filter
  -> create deliveries
  -> create send_delivery jobs
  -> poke Dispatcher DO
  -> return 201 with counts
```

Recommended direct-recipient cap: `100`.

Rationale:

- User intent: transactional emails go to single users or small numbers of recipients.
- If callers need thousands of recipients, they should use contacts/lists/campaign flow.
- The technical limit can be raised later without architecture changes.

Preserve existing direct-recipient error semantics:

- malformed JSON: `400 invalid_request`
- invalid payload: `400 invalid_request`
- empty recipients: `400 invalid_request`
- all recipients suppressed / no sendable recipients: `422 empty_recipient_set`

### List/campaign path

For list-based mailings, the eventual flow is:

```txt
POST /api/mailings { listId: "..." }
  -> create mailing
  -> create expand_mailing job
  -> poke Dispatcher DO
  -> return 202-style response with mailing id and expansion state
```

`expand_mailing` means:

```txt
resolve list audience -> snapshot recipients -> apply suppressions -> create deliveries -> create send_delivery jobs
```

Expansion must be chunked, resumable, and idempotent:

- Read list members by cursor.
- Use `jobs.payload_json` for cursor state, e.g. `{ "afterContactId": "..." }`.
- Insert deliveries/jobs in D1 batches respecting the 100-bound-param limit.
- Chunk’s delivery inserts, `send_delivery` job inserts, and cursor advancement / next expansion job insert must happen in one `env.DB.batch()`.
- Unique `deliveries(mailing_id, email)` is the final defense against duplicate expansion.
- Queue the next `expand_mailing` job if more recipients remain.
- Do not send; only create `send_delivery` jobs.

### Marketing guard

Because unsubscribe support is not implemented yet, reject marketing mailing creation for now:

```json
{
  "error": {
    "code": "marketing_unsubscribe_not_available",
    "message": "Marketing sending is disabled until Nusend has unsubscribe support."
  }
}
```

Implication for this migration:

- Direct transactional sends are fully usable.
- List/campaign API is architecturally prepared but marketing list campaigns remain blocked until unsubscribe support exists.
- `expand_mailing` should still be implemented and tested directly with seeded data/jobs so the Dispatcher pipeline is proven without enabling marketing sends.

This guard should be removed only when real unsubscribe support exists.

### Mailing state transitions for this migration

Define and test minimal transitions:

- Direct-recipient transactional mailing starts as `scheduled` and remains `scheduled` while `send_delivery` jobs are queued because SES sender is not implemented.
- List-based mailing created internally for expansion starts as `scheduled`.
- When expansion completes, the mailing may move to `sending` or remain `scheduled` until sender exists; choose one in implementation and document it in tests. Recommended for this migration: keep it `scheduled` until an actual sender starts processing deliveries, to avoid implying sends are in progress.
- Future sender moves mailings to `sending` and eventually `completed`.

### Read endpoint

Add `GET /api/mailings/:id` during migration.

Reason:

- List-based creation is async in the target architecture and cannot immediately return final delivery counts.
- A read endpoint makes expansion progress observable and testable.

Return minimal state and counts, e.g.:

```json
{
  "mailing": {
    "id": "...",
    "purpose": "transactional",
    "state": "scheduled",
    "scheduledAt": "..."
  },
  "counts": {
    "deliveries": 0,
    "queued": 0,
    "suppressed": 0,
    "sent": 0,
    "failed": 0
  }
}
```

## Dispatcher Durable Object

Add real structural Dispatcher DO code now, but no SES sender.

### Responsibilities in this migration

- Expose a `poke()` RPC method to schedule an alarm soon if registered-kind work exists.
- `alarm()` processes registered job kinds.
- Registered initially: `expand_mailing`.
- Not registered yet: `send_delivery`.
- `send_delivery` jobs remain queued and untouched.
- Re-arm alarm if registered-kind work remains.
- Stop if no registered-kind work remains.

Use modern Durable Object RPC-style methods where practical instead of inventing a fetch sub-router inside the DO.

### Processor registry

`src/dispatch/processors.ts`:

```ts
export const processors = {
  expand_mailing: createExpandMailingProcessor(...),
};
```

The dispatcher must always claim with the registered kind list.

Important safety rule:

- If processor registry is empty, claim nothing.
- Never claim `send_delivery` until a real sender exists.
- Never mark `send_delivery` succeeded without sending.

### `queue/runner.ts` role

Repurpose `queue/runner.ts` as pure reusable queue tick logic called by the Dispatcher:

```txt
claim registered jobs -> process -> complete/fail -> summarize result
```

This keeps the Durable Object class thin and makes the claim/process/complete behavior easier to test outside the DO wrapper.

### Scheduled watchdog behavior

The Worker `scheduled()` handler should do only:

1. release expired leases
2. if queued jobs of registered kinds exist, poke the Dispatcher DO

It should not process jobs directly.

### Future SES sender phase

Later, register `send_delivery` processor:

- global SES token bucket inside Dispatcher DO
- max rate from config / optional `GetSendQuota`
- send via SES v2 signed HTTPS (`aws4fetch`)
- write `send_attempts`
- update `deliveries`
- retry/dead-letter via D1 queue

## Queue Port to D1

Rewrite `queue/jobs.ts` for D1:

- async functions
- `D1Database` instead of `bun:sqlite`
- positional params only
- retain atomic `UPDATE ... RETURNING` claim pattern
- retain leases and retry/dead-letter behavior
- order claims by priority:

```sql
ORDER BY priority DESC, run_at ASC, created_at ASC, id ASC
```

Port operations:

- `claimJobs`
- `completeJob`
- `failJob`
- `releaseExpiredLeases`
- `hasQueuedJobsByKinds`
- helpers that build `D1PreparedStatement`s for transitions that must be composed into larger `env.DB.batch()` calls, especially expansion chunk completion

Keep `queue/backoff.ts` and `queue/time.ts` mostly unchanged.

## Public Route Contracts

Add public unauthenticated route stubs:

- `GET /unsubscribe/:token`
- `POST /unsubscribe/:token`
- `POST /webhooks/ses`

For this migration, return `501 not_implemented` with structured JSON or minimal text/HTML as appropriate.

Tests must assert:

- these routes are public
- no API key is required
- they return explicit `501`

Future behavior documented in `PROJECT.md`:

- unsubscribe verifies signed token, updates suppressions/list membership
- webhook verifies SNS signature and expected TopicArn, stores raw event, processes bounce/complaint/delivery idempotently

## Testing Plan

Switch entirely to official Cloudflare Workers Vitest integration:

- add `@cloudflare/vitest-pool-workers`
- configure `vitest.config.ts` with Workers pool and `wrangler.jsonc`
- apply D1 migrations in test setup
- remove Bun subprocess helper

Use official Workers/Durable Object test APIs, including alarm test helpers such as `runDurableObjectAlarm` / equivalent provided by the installed Cloudflare test integration. Do not add ad-hoc production test-only dispatcher endpoints.

Test groups:

### Pure tests

- `mailings/validation.test.ts`
- `queue/backoff.test.ts`
- `queue/time.test.ts`

Run them in Workers pool too for consistency.

### D1 integration tests

- API key auth lookup/revocation.
- Queue claim/complete/fail/release.
- Queue priority ordering.
- Dispatcher claims only registered kinds.
- Dispatcher does not touch `send_delivery` jobs until sender registered.
- `expand_mailing` chunking and resumability.
- Expansion idempotency: retrying the same chunk does not create duplicate deliveries or duplicate send jobs.

### Route tests

- `/health` works and is public.
- `/api/mailings` requires API key.
- Direct-recipient transactional mailing creates deliveries + `send_delivery` jobs.
- Marketing creation returns `422 marketing_unsubscribe_not_available` until unsubscribe exists.
- Public unsubscribe/webhook stubs are accessible without API key and return `501`.
- `GET /api/mailings/:id` returns minimal counts.

## Implementation Phases

### Phase 1: Repo flattening and minimal Cloudflare scaffold

- Move to single root package.
- Delete `apps/cli` and `apps/service` package shell.
- Delete superseded untracked VPS SES plan files:
  - `.plans/add-ses-sender-runtime-worker.md`
  - `.plans/add-ses-sender-runtime-worker.html`
- Add root `src/`, `migrations/`, `scripts/`.
- Add `wrangler.jsonc`.
- Add minimal compilable `src/index.ts` that exports:
  - `fetch` with a temporary/basic app or health response
  - `scheduled` no-op
  - stub `Dispatcher` class
- Add root `tsconfig.json` for Workers.
- Update `package.json` scripts:
  - `dev`: `wrangler dev`
  - `deploy`: `wrangler deploy`
  - `db:migrate:local`: `wrangler d1 migrations apply nusend --local`
  - `db:migrate:remote`: `wrangler d1 migrations apply nusend --remote`
  - `typecheck`
  - `test`
  - `check`
- Remove Bun scripts and `@types/bun`.

### Phase 2: Fresh D1 schema

- Add `migrations/0001_init.sql`.
- Rebaseline current useful domain schema.
- Remove current custom migration files.
- Add `api_keys`, flexible `jobs`, `priority`, `payload_json`, and `deliveries_mailing_email_idx`.
- Drop templates and Better Auth tables.

### Phase 3: Workers testing foundation

- Add `@cloudflare/vitest-pool-workers`.
- Configure Vitest for Workers runtime.
- Add migration test setup.
- Port pure tests first.

### Phase 4: API-key auth

- Add API-key hashing/verification middleware.
- Add key generation script.
- Add auth tests.

### Phase 5: Hono Worker app

- Replace temporary app with `src/app.ts` Hono assembly.
- Add `src/bindings.ts` types.
- Add `/health`.
- Add public route stubs.
- Add auth-protected `/api` routing boundary.

### Phase 6: D1 queue and Dispatcher DO

- Port queue jobs module to D1.
- Repurpose `queue/runner.ts` for reusable queue tick logic.
- Flesh out Dispatcher Durable Object.
- Add processor registry.
- Add scheduled watchdog behavior.
- Add tests for registered-kind safety and alarm-driven processing.

### Phase 7: Mailings migration

- Port validation.
- Rewrite direct-recipient create path for D1 batch inserts.
- Add marketing guard.
- Add `expand_mailing` processor and direct tests with seeded data/jobs.
- Add `GET /api/mailings/:id`.
- Add tests.

### Phase 8: Documentation rewrite

Rewrite `PROJECT.md` as Cloudflare-first, no pivot narrative.

Update `README.md` with:

- Cloudflare setup
- D1 migration commands
- API key generation
- local dev
- current implemented scope
- future SES/unsubscribe/webhook/template phases

## `PROJECT.md` Rewrite Requirements

`PROJECT.md` should describe:

- Product: API-only email orchestration service built on AWS SES and Cloudflare.
- Deployment: user’s Cloudflare account + AWS SES; Workers Paid expected.
- Stack:
  - TypeScript
  - Cloudflare Workers
  - Hono
  - D1
  - Durable Objects
  - Wrangler migrations
  - AWS SES v2 via signed HTTPS/future `aws4fetch`
- Auth:
  - API keys only
  - server-generated
  - SHA-256 hash at rest
- Data model:
  - mailings/deliveries
  - contacts/lists/suppressions
  - jobs/send_attempts/ses_events
  - no orgs/workspaces
- Sending pipeline:
  - direct recipients materialized immediately
  - list recipients expanded asynchronously via `expand_mailing`
  - Dispatcher DO rate-limits future SES sends
  - cron every 5 minutes as watchdog
- Queue design:
  - D1 source of truth
  - leases/retries/dead-letter
  - flexible job kinds
  - job priority for transactional work
- Templates/content:
  - final HTML/text only
  - future placeholders via `vars_json`
  - future stored template API copy-on-create
  - no Markdown source stored
- Public routes:
  - unsubscribe route
  - SES/SNS webhook
- Roadmap/build phases:
  1. Cloudflare foundation migration
  2. SES sender with Dispatcher rate limiter
  3. unsubscribe implementation
  4. SNS webhook verification/event processing
  5. contacts/lists/campaign API expansion
  6. templates API and placeholder rendering
  7. CLI wrapper

## Validation

After implementation, run:

```sh
pnpm install
pnpm format
pnpm check
pnpm test
pnpm dev
```

Also verify:

- Wrangler D1 local migration applies.
- `/health` works in `wrangler dev` and is public.
- API key auth protects `/api/*`.
- Public routes are not protected.
- Direct transactional mailing creates queued `send_delivery` jobs.
- `send_delivery` jobs remain queued because SES sender is not implemented.
- `expand_mailing` processor can expand seeded list jobs into queued `send_delivery` jobs in tests.

## Risks and Mitigations

### Expansion idempotency

Risk: Durable Object alarms are at least once. If expansion retries a chunk after partial work, duplicate deliveries/jobs could be created.

Mitigation:

- chunk delivery inserts, send job inserts, and cursor advancement must be one D1 `batch()`
- add unique index on `(mailing_id, email)`
- write explicit idempotency tests

### D1 limits during expansion

Risk: D1 100 bound params and per-invocation query limits.

Mitigation:

- chunk recipient reads and multi-row inserts
- keep direct `recipients[]` cap at 100
- use list expansion for large audiences
- bounded Dispatcher processing slices

### Transactional latency during campaigns

Risk: campaign work blocks password resets.

Mitigation:

- direct transactional materialization
- `jobs.priority`
- future sender claims high-priority jobs first

### Fake processing of send jobs

Risk: queued emails marked succeeded without SES.

Mitigation:

- `send_delivery` not registered in dispatcher registry until real sender exists
- explicit tests for this

### Marketing without unsubscribe

Risk: compliance failure.

Mitigation:

- reject marketing creation until unsubscribe route is implemented

### Durable Object alarm at-least-once behavior

Risk: duplicated processing after retry.

Mitigation:

- D1 job leases
- idempotent processors
- state checks

### Future schema evolution

Risk: CHECK constraints block new states/kinds.

Mitigation:

- avoid CHECK constraints on evolvable workflow state/kind columns
- validate in TypeScript

## Out of Scope for This Migration

- Actual SES `SendEmail` calls.
- SES token bucket implementation.
- SNS signature verification and event processing.
- Real unsubscribe token generation/handling.
- Template CRUD API.
- Placeholder rendering.
- CLI implementation.
- R2 assets.
- Dashboard/admin UI.
